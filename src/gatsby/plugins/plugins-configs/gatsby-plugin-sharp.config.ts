const gatsbyPluginSharp = {
    resolve: `gatsby-plugin-sharp`,
    options: {
        defaults: {
            formats: [`auto`],
            placeholder: `blurred`,
            backgroundColor: `transparent`,
        },
    },
};

export default gatsbyPluginSharp;
